<% if (site.tags.length){ %>
  <div class="widget-wrap"><!--标签-->
    <div class="widget">
      <h3 class="widget-title"><%= __('tags') %></h3>
      <%- list_tags({
        show_count: theme.show_count,
        class: 'widget-tag'
      }) %>
    </div>
  </div>
<% } %>
